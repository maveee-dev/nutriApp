export interface FoodRecognitionNutritionEstimate { nutrient: string; amount: string; unit: string; basis: string; }
export interface FoodRecognitionAlternative { foodId: string; displayName: string; variantLabel: string | null; canonicalName: string | null; }
export interface FoodRecognitionServingSuggestion { label: string; grams: string | null; }
export interface FoodRecognitionCandidate { label: string; confidence: number; foodId: string | null; foodName: string | null; foodDisplayName?: string | null; foodVariantLabel?: string | null; matchStatus: 'database-match' | 'ambiguous' | 'ai-estimate' | 'unmatched'; resolutionStatus: 'matched' | 'ambiguous' | 'unmatched'; nutritionSource: 'canonical-database' | 'ai-estimated' | null; requiresReview: boolean; alternatives?: FoodRecognitionAlternative[]; servingSuggestion?: FoodRecognitionServingSuggestion; estimatedNutrition?: FoodRecognitionNutritionEstimate[]; }
export interface FoodRecognitionImageQuality { status: 'good' | 'needs-review' | 'poor' | 'unavailable'; issues: string[]; }
export interface FoodRecognitionResponse { apiVersion: string; providerId: string; providerAvailable: boolean; recognitionStatus: 'completed' | 'unavailable'; imageQuality: FoodRecognitionImageQuality; mealConfidence: number | null; mealDescription: string | null; candidates: FoodRecognitionCandidate[]; limitations: string[]; }
export interface FoodRecognitionRequest { imageData: string; mimeType: string; }
